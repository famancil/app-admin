<%= form_with(model: corporacion, local: true) do |form| %>
  <% if corporacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(corporacion.errors.count, "error") %> prohibited this corporacion from being saved:</h2>

      <ul>
      <% corporacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :rut %>
    <%= form.text_field :rut %>
  </div>

  <div class="field">
    <%= form.label :contraseña %>
    <%= form.text_field :contraseña %>
  </div>

  <div class="field">
    <%= form.label :sucursal %>
    <%= form.text_field :sucursal %>
  </div>

  <div class="field">
    <%= form.label :filas %>
    <%= form.text_field :filas %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
